# Demo Accessibility Notes

- All primary actions should expose a meaningful `accessibilityLabel`.
- Interactive elements should target at least 44x44 points.
- Prefer semantic text styles so the demo remains usable at large content sizes.
- Test navigation order with VoiceOver after adding new screens.
